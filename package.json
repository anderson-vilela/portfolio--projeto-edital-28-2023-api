{
  "scripts": {
    "node:start": "json-server-auth db.json --host 0.0.0.0 --port 3000",
    "docker:build": "docker build -t anderson-vilela/portfolio-lais-huol-api:v1.0 .",
    "docker:run": "docker run --rm -p 3000:3000 -it anderson-vilela/portfolio-lais-huol-api:v1.0"
  },
  "devDependencies": {
    "json-server": "^0.16.3",
    "json-server-auth": "^2.1.0"
  }
}
